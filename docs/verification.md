# Результаты проверки — 24 сентября 2026

- `./scripts/verify.sh -B -ntp`: BUILD SUCCESS.
- Модульные тесты: 10, интеграционные тесты: 12. Ошибок и пропусков: 0.
- JaCoCo: покрыто 533 из 541 строк — **98.52%** при требовании >= 70%.
- Интеграционные тесты используют PostgreSQL 17 через Testcontainers; рабочая БД не затрагивается.
- Проверены гонки двух передач одного животного и двух записей на последнее место, а также полный откат передачи при ошибке БД.
- `./scripts/compose.sh config --quiet`: конфигурация валидна.
- `./scripts/compose.sh up --build -d --wait`: образ собран внутри Docker, app и db имеют статус healthy.
- Runtime контейнера: Eclipse Temurin 21.0.12. Maven-тесты локально выполнены на Temurin 25.0.4 с компиляцией под Java 21.
- `python3 scripts/demo.py`: result OK, оба бизнес-сценария пройдены через HTTP.
- `/actuator/health`, `/swagger-ui/index.html`, `/v3/api-docs`: HTTP 200.
- IntelliJ IDEA открыл и импортировал проект; установлен SDK temurin-25, language level 21.

В рабочей БД оставлен демонстрационный набор: приют 1, животное 1, заявки 1/2, смена 1, участия 1/2. Контейнеры оставлены запущенными для просмотра Swagger.

Согласование схемы с преподавателем и проверка вторым участником не имитировались. Материалы для этих шагов находятся в `architecture.md`, `requirements.md` и `defense.md`.
