spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_batch_fetch_size: 50
  jackson:
    deserialization:
      fail-on-unknown-properties: true
logging:
  level:
    root: WARN
    org.testcontainers: INFO
